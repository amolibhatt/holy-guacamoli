## Packages
framer-motion | Smooth animations for quiz interactions and page transitions
lucide-react | Beautiful icons for UI elements (already in base but explicit check)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
